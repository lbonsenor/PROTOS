Agregar una ruta: `sudo route add -net <red> netmask <máscara> gw <gateway>`

Eliminar una ruta: `sudo route del -net <red> netmask <máscara> gw <gateway>`

Cambiar o agregar la ruta por defecto: `sudo route add default gw <gateway>` o `sudo route del default`
